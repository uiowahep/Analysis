#/bin/bash

for f in crab_auto*.py; do
    echo
    echo "------------------------------------------------------"
    echo "submitting $f"
    crab submit -c "$f"
    echo "------------------------------------------------------"
    echo
done
