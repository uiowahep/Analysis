#/bin/bash

for f in crab_projects/*; do
    echo
    echo "------------------------------------------------------"
    echo "submitting $f"
    crab resubmit -d "$f"
    echo "------------------------------------------------------"
    echo
done
